version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    image: tv-devops-app:local
    container_name: tv-devops-app
    environment:
      - NODE_ENV=development
      - PORT=3000
    ports:
      - "3000:3000"
